{
  "name": "huangsam.github.io",
  "description": "Holds links to my commonly used pages",
  "license": "UNLICENSED",
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "ajv": "*",
    "babel-loader": "*",
    "css-loader": "*",
    "eslint": "*",
    "husky": "*",
    "jest": "*",
    "mini-css-extract-plugin": "*",
    "node-sass": "*",
    "sass-loader": "*",
    "style-loader": "*",
    "webpack": "^4.16.2",
    "webpack-cli": "^3.1.0"
  },
  "scripts": {
    "lint": "eslint app",
    "test": "jest --coverage",
    "dev:build": "webpack --progress",
    "prod:build": "webpack -p --progress"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint && yarn test",
      "pre-push": "yarn lint && yarn test && yarn dev:build"
    }
  }
}
